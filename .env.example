# Copie para .env (usado pelo docker-compose)
POSTGRES_DB=stoprotas
POSTGRES_USER=stoprotas
POSTGRES_PASSWORD=stoprotas

# Segredo do JWT — TROQUE em produção
JWT_SECRET=troque-este-segredo-em-producao

# Só para produção com Caddy (docker-compose.prod.yml): seu domínio para o HTTPS
DOMAIN=api.seudominio.com.br
