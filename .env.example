# Copie para .env (usado pelo docker-compose)
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=stoprotas
MYSQL_USER=stoprotas
MYSQL_PASSWORD=stoprotas

# Segredo do JWT — TROQUE em produção
JWT_SECRET=troque-este-segredo-em-producao

# Só para produção (docker-compose.prod.yml): seu domínio para o HTTPS do Caddy
DOMAIN=api.seudominio.com.br
